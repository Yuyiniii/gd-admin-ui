# 生产环境配置

# 应用标题
VITE_APP_TITLE = 管理系统

# 生产环境端口（如果需要本地打包预览）
VITE_PORT = 9090

# 生产环境 API 地址
VITE_API_BASE_URL = http://localhost:9090

# API 前缀
VITE_API_PREFIX = /prod-api

# 是否启用代理（生产环境通常不需要）
VITE_PROXY_ENABLED = false